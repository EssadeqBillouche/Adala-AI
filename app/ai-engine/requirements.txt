# =============================================================================
# AdalaAI Legal Engine — Python Dependencies
# =============================================================================

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.3
pydantic-settings>=2.1.0          # Centralized config with env validation

# --- Vector database ---
qdrant-client>=1.12.0             # Hybrid search support (dense + sparse)

# --- Embedding models ---
fastembed>=0.3.0                  # SPLADE sparse embeddings
sentence-transformers>=3.0.0      # BGE-M3 dense embeddings
# NOTE: sentence-transformers pulls torch (~2 GB).  For CPU-only Docker
# builds, add: --extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
transformers>=4.40.0

# --- LLM orchestration ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0

# --- HTTP client ---
httpx>=0.27.0                     # Sync + async, used for Gemini / OpenAI calls

# --- Async utilities ---
aiohttp>=3.10.0

# --- PDF processing ---
PyPDF2>=3.0.0                     # PDF text extraction (legacy pipeline)
docling>=2.0.0                    # Modern PDF-to-Markdown with table & layout support
tqdm>=4.65.0                      # Progress bars for batch processing
